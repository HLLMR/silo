# ── Node / frontend ──
node_modules/
dist/
dist-ssr/
.vite/
*.local

# ── Rust / Tauri ──
src-tauri/target/
target/
src-tauri/gen/
**/*.rs.bk
*.pdb

# ── Build output ──
release/
out/
*.exe
*.dmg
*.msi
*.app
*.zip

# ── Databases / caches (app runtime data) ──
*.db
*.db-wal
*.db-shm
*.sqlite
*.sqlite3

# ── Logs & debug artifacts ──
# (Lesson from the incumbent: never commit scan logs, dumps, or scratch.)
logs/
*.log
crash-log.txt
detection_log.txt
*.bin
scratch/
tmp/
temp/

# ── Local reference mirror (root-anchored; keeps docs/reference/ committed) ──
# GIANTS' copyrighted SDK schemas + the user's real savegame/mod samples live
# here for local dev only. Never redistribute them in the public repo — the
# distilled facts go in docs/reference/ instead.
/reference/

# ── Env / secrets ──
.env
.env.*
!.env.example

# ── IDE / OS ──
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db
